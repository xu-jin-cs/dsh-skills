# Gumroad 英文上架文案

## 商品 1：全家桶（主推）

**Product name:** Agent Workflow Gates — 57 Production-Ready AI Agent Skills

**Tagline:** Mechanical enforcement gates, parallel dispatch, and workflow rules for AI coding agents. Stop your agent from claiming done without proof.

**Description:**

```
Your AI agent says "done." Is it actually done?

This package is a battle-tested governance system for AI coding agents
(Kimi Code / Claude Code / OpenClaw compatible): 57 composable skills that
turn vague instructions into mechanically enforced gates.

WHAT'S INSIDE — 57 SKILLS IN 3 LAYERS

▸ 7 Framework Skills
  gate-switch (universal claim-verification engine), dual-gates
  (declare + query gates), plan-select (3-slot plan arbitration),
  parallel-dispatch (fan-out switch + seal gate + merge checks),
  bug-fix-strategy, scope-boundary-gate, idea-forge

▸ 46 Enforcement Gates (each a standalone, self-contained skill)
  Every time your agent claims "X is written / synced / verified / fixed",
  a gate mechanically checks X before letting it pass:
  - danger_cmd_gate: blocks rm -rf & destructive commands BEFORE execution
  - problem_gate: routes any problem through a root-cause decision tree
  - zero_residual / no_abs_path / func_signature / field_consumer:
    code-change verification gates
  - loop_fuse / task_launch_gate: loop breakers that stop token burn
  - merge_gate / receipt_gate / shard_result_gate: parallel sub-agent
    contract enforcement
  - prd_inputs / deploy_admission / testlead_dispatch: pipeline admission
    gates
  ...and 35 more, each with its own SKILL.md, spec, and check scripts.

▸ 4 Workflow Rule Packs (pure-instruction skills)
  workflow-governance, roles-and-standards, test-quality-system,
  governance-archive — the complete rule corpus that the gates enforce.

WHY IT WORKS
Prompts alone fail probabilistically. These gates don't ask the model to
behave — they execute: file_exists / json_field / grep_count / script_exit
checks with four-state verdicts (A=pass, B=block, CLARIFY, VIOLATION).
Block reasons are auto-generated; the agent copies the verdict, never
writes its own.

COMPATIBILITY
- Any agent runtime that loads SKILL.md (agentskills.io spec)
- Kimi Code / Claude Code / OpenClaw compatible
- Python 3, zero third-party dependencies (jieba optional for dual-gates)
- No DSH/Harness runtime required
- No hardcoded personal paths or private infrastructure dependencies
- Every skill is self-contained: buy one, run one

INSTALL
Copy any skill folder into your agent's skills directory
(e.g. ~/.agents/skills/). Done.

License: commercial use allowed for the buyer's own projects;
redistribution/resale of the package itself is not permitted.
```

**价格建议:** $39（可接受区间 $29~49）

---

## 商品 2：体验合集（引流款）

**Product name:** Agent Workflow Gates — Starter Pack (15 Skills)

**Tagline:** 15 essential enforcement skills for AI coding agents. The full 57-skill system, distilled.

**Description:**

```
A starter slice of the Agent Workflow Gates system — 15 skills:

▸ All 7 framework skills (gate-switch engine, dual-gates, plan-select,
  parallel-dispatch, bug-fix-strategy, scope-boundary-gate, idea-forge)
▸ 4 workflow rule packs (the full rule corpus in references/)
▸ 4 most universal gates: danger_cmd_gate, problem_gate, zero_residual,
  no_abs_path

Everything runs standalone with Python 3 and no vendor-specific runtime.
If it changes how your agent behaves (and it will), upgrade to the full
57-skill bundle — contact me for an upgrade discount code.
```

**价格建议:** $9

---

## 上架填表速查

| 字段 | full | lite |
|---|---|---|
| 类型 | Digital Download | Digital Download |
| 文件 | store-package-full.zip (528K) | store-package-lite.zip (221K) |
| 建议价 | $39 | $9 |
| 标签 | ai-agents, claude-code, kimi-code, agent-skills, devops, prompt-engineering | 同左 |
